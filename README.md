# Doctrine Extra

Extra utilities for Doctrine: DBAL types, ORM helpers (entity handler, queries, graph schema, commands), collections and data fixtures support.

## Installation

```bash
composer require codraw/doctrine-extra
```
